from sikwidgets.widgets.widget import Widget
from sikwidgets.widgets.widget import WidgetError

# Perhaps a tree should just be a tree data structure made out of buttons, or TreeCells
# Keep It Simple. Just know that if a parent does not exist, the child won't. Things like
# that. Although, should this be assumed like that?
class Tree(Widget): pass